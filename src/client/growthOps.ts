/** Growth operations are deferred (T1.8) — no live sample captured yet. */

export function throwGrowthNotYetImplemented(): never {
  throw new Error("Growth operations not yet implemented (T1.8 deferred)");
}
